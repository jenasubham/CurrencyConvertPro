// All the imports will come here and we can export all the imports directly, 
// so that in the App file there will not be mess like so many import lines.

import InputBox from './InputBox';

export {InputBox};
